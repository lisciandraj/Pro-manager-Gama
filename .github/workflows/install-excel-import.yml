name: Legacy Excel installer (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Excel installer disabled; deploy-gama-ui.yml is the single index mutator."
