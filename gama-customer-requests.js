/* GAMA — Solicitudes de clientes */
(function(){'use strict';
const C=()=>window.GamaCloud,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>Number(v||0).toLocaleString('es-EC',{style:'currency',currency:'USD',minimumFractionDigits:2,maximumFractionDigits:2});
const role=()=>{try{return JSON.parse(localStorage.getItem('gama_session_v1')||'null')?.role||''}catch(_){return ''}};
let requests=[],customers=[],products=[],page=0,totalCount=0,searchTimer=null,generation=0;
const PAGE_SIZE=20;
function isSearching(){return (document.getElementById('crSearch')?.value||'').trim().length>0}
function staff(){return ['admin','commercial','administrador','comercial'].includes(role())&&!!window.gamaAccessAllowed?.('customer-requests')}
function css(){if(document.getElementById('gamaCustomerRequestsCss'))return;const s=document.createElement('style');s.id='gamaCustomerRequestsCss';s.textContent=`.cr{background:#fff;border:1px solid #E2E8EC;border-radius:16px;padding:16px;box-shadow:0 3px 16px #1732460d}.crTools,.crDetailHead{display:flex;justify-content:space-between;align-items:center;gap:12px}.crTools{margin:16px 0}.crTools input{flex:1}.crTable{overflow:auto;border:1px solid #E4EBEE;border-radius:12px}.crTable table{width:100%;min-width:850px;border-collapse:collapse;display:table;white-space:normal}.crTable th,.crTable td{padding:11px 12px;border-bottom:1px solid #EDF1F2;text-align:left;font-size:12px}.crTable th{font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:#71808A;background:#F8FAFB}.crNum{font-family:ui-monospace,monospace;font-weight:900;color:#F47A2A}.crStatus{display:inline-block;padding:5px 8px;border-radius:999px;font-weight:800;background:#EEF3F4;color:#60717B}.crStatus.pending{background:#FFF6E8;color:#B66A18}.crStatus.accepted{background:#E8F5F6;color:#087C8B}.crStatus.invoiced{background:#E7F6F0;color:#138A69}.crStatus.cancelled,.crStatus.rejected{background:#FFF0EC;color:#C94F45}.crDetail{margin-top:14px;padding:15px;border:1px solid #E2E8EC;border-radius:14px;background:#F8FAFB}.crLines{margin-top:12px;background:#fff;border:1px solid #E4EBEE;border-radius:10px;overflow:auto}.crLines table{width:100%;border-collapse:collapse}.crLines th,.crLines td{padding:9px;border-bottom:1px solid #EDF1F2;font-size:12px;text-align:left}.crLines td small{display:block;color:#60717B;margin-top:3px}.crTotal{display:flex;justify-content:flex-end;gap:25px;font-size:17px;font-weight:900;padding:12px}.crActions{display:flex;justify-content:flex-end;gap:8px;margin-top:12px}.crModal{position:fixed;inset:0;z-index:99990;background:#18324a66;display:grid;place-items:center;padding:18px}.crModalBox{width:min(560px,100%);background:#fff;border-radius:16px;padding:20px;box-shadow:0 20px 60px #18324a33}.crModalBox h3{margin-top:0}.crModalBox select{width:100%;margin:7px 0 12px}.crMsg{margin-top:9px;font-size:12px;font-weight:700}.crOk{color:#138A69}.crErr{color:#C94F45}.crEmpty{text-align:center;padding:28px;color:#71808A}.crNote{margin-top:10px;padding:10px;background:#FFF6EF;border-left:4px solid #F47A2A;border-radius:7px;font-size:12px}.crPager{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-top:10px;font-size:12px;color:#71808A}@media(max-width:700px){.crTools,.crDetailHead{align-items:stretch;flex-direction:column}.crActions{display:grid;grid-template-columns:1fr}}`;document.head.appendChild(s)}
function ensure(){
 css();const sec=document.getElementById('gqRequests');if(!sec)return null;
 if(sec.dataset.ready)return sec;sec.dataset.ready='1';
 sec.innerHTML='<div class="cr"><div class="crTools"><input id="crSearch" data-gi-placeholder=aaf1a355cf20 placeholder="Buscar por cliente, correo o número…"><span class="badge" id="crCount"></span><button class="secondary" id="crRefresh" data-gi-live data-gi=fe5f6628c7b5>Actualizar</button></div><div id="crRows"></div><div id="crPager" class="crPager"></div><div id="crDetail"></div></div>';
 document.getElementById('crRefresh').onclick=()=>load();
 document.getElementById('crSearch').oninput=()=>{clearTimeout(searchTimer);searchTimer=setTimeout(()=>{page=0;load()},300)};
 return sec;
}
async function load(){if(!C()||!staff())return;const root=ensure();if(!root)return;const token=++generation;const rows=document.getElementById('crRows');if(rows)rows.innerHTML='<div class="crEmpty"><span class="gamaSpin"></span>Sincronizando con la nube…</div>';const q=(document.getElementById('crSearch')?.value||'').trim();try{
  const cr=await C().list('customers',{order:'name',ascending:true});if(cr.error)throw cr.error;customers=cr.data||[];
  const pr=await C().list('products',{select:'id,name,reference,barcode,category,sale_price,tax_rate,stock,active',order:'name',ascending:true});if(pr.error)throw pr.error;products=pr.data||[];
  let rowsData=[];
  if(q){
    const ql=q.toLowerCase();
    const matchIds=customers.filter(c=>[c.name,c.identification,c.email].some(v=>String(v||'').toLowerCase().includes(ql))).map(x=>x.id);
    const queries=[C().list('customer_requests',{ilike:{requester_name:'%'+q+'%'},order:'created_at',ascending:false,limit:200}),C().list('customer_requests',{ilike:{requester_email:'%'+q+'%'},order:'created_at',ascending:false,limit:200})];
    if(matchIds.length)queries.push(C().list('customer_requests',{in:{customer_id:matchIds},order:'created_at',ascending:false,limit:200}));
    const results=await Promise.all(queries);
    for(const r of results)if(r.error)throw r.error;
    const byId=new Map();results.forEach(r=>(r.data||[]).forEach(x=>byId.set(String(x.id),x)));
    rowsData=[...byId.values()].sort((a,b)=>new Date(b.created_at)-new Date(a.created_at));
    totalCount=rowsData.length;
  }else{
    const rr=await C().list('customer_requests',{order:'created_at',ascending:false,range:[page*PAGE_SIZE,page*PAGE_SIZE+PAGE_SIZE-1],count:'exact'});if(rr.error)throw rr.error;
    rowsData=rr.data||[];totalCount=rr.count||0;
  }
  if(token!==generation||!root.isConnected||!staff())return;requests=rowsData;render()
}catch(e){console.error('[GAMA Requests]',e);if(rows&&root.isConnected&&token===generation)rows.innerHTML='<div class="crEmpty crErr" data-gi=1ad05f5bd7a7>No se pudieron cargar las solicitudes. Verifica las políticas RLS.</div>'}}
const customer=id=>customers.find(x=>String(x.id)===String(id)),product=id=>products.find(x=>String(x.id)===String(id));
const clientName=r=>customer(r.customer_id)?.name||r.requester_name||'Cliente no vinculado',clientEmail=r=>customer(r.customer_id)?.email||r.requester_email||'';
const statusLabel=x=>({pending:'Pendiente',accepted:'Aceptada',invoiced:'Convertida en presupuesto',rejected:'Rechazada',cancelled:'Cancelada'}[x]||x||'Pendiente');
function renderPager(){const host=document.getElementById('crPager');if(!host)return;if(isSearching()||!totalCount){host.innerHTML='';return}const from=page*PAGE_SIZE+1,to=Math.min(totalCount,(page+1)*PAGE_SIZE);host.innerHTML=`<span>${from}–${to} de ${totalCount}</span><span><button class="secondary" type="button" id="crPrev" ${page<=0?'disabled':''}>‹ Anterior</button> <button class="secondary" type="button" id="crNext" ${to>=totalCount?'disabled':''}>Siguiente ›</button></span>`;document.getElementById('crPrev').onclick=()=>{if(page>0){page--;load()}};document.getElementById('crNext').onclick=()=>{if(to<totalCount){page++;load()}}}
function render(){if(!ensure())return;const arr=requests;document.getElementById('crCount').textContent=totalCount+' solicitud'+(totalCount===1?'':'es');const host=document.getElementById('crRows');if(!host)return;host.innerHTML=arr.length?`<div class="crTable"><table><thead><tr><th data-gi=213b026e9aa4>Solicitud</th><th data-gi=93b2a9ef782c>Fecha</th><th data-gi=f851d9a83ab0>Cliente</th><th data-gi=98e5acddb6c4>Estado</th><th data-gi=4c65f6bb0d0c>Total sin IVA</th><th></th></tr></thead><tbody>${arr.map(r=>`<tr><td><span class="crNum">${esc(r.dossier_reference||String(r.id).slice(0,8).toUpperCase())}</span></td><td>${esc(r.created_at?new Date(r.created_at).toLocaleString('es-EC'):'-')}</td><td><b>${esc(clientName(r))}</b><br><small>${esc(clientEmail(r))}</small></td><td><span class="crStatus ${esc(r.status)}">${esc(statusLabel(r.status))}</span></td><td><b>${money(r.total)}</b></td><td><button class="secondary" data-cr-open="${esc(r.id)}" data-gi=2b1774ff8bae>👁️ Detalle</button></td></tr>`).join('')}</tbody></table></div>`:`<div class="crEmpty">${isSearching()?'Sin resultados para tu búsqueda.':'No hay solicitudes de clientes.'}</div>`;host.querySelectorAll('[data-cr-open]').forEach(b=>b.onclick=()=>detail(b.dataset.crOpen));renderPager()}
async function detail(id){if(!staff()||!ensure())return;const r=requests.find(x=>String(x.id)===String(id));if(!r)return;const host=document.getElementById('crDetail');host.innerHTML='<div class="crDetail"><span class="gamaSpin"></span>Cargando detalle…</div>';try{const lr=await C().list('customer_request_lines',{eq:{request_id:r.id},order:'created_at',ascending:true});if(lr.error)throw lr.error;const lines=lr.data||[];if(!host.isConnected||!staff())return;host.innerHTML=`<div class="crDetail"><div class="crDetailHead"><div><b><span data-gi=213b026e9aa4>Solicitud </span>${esc(r.dossier_reference||String(r.id).slice(0,8).toUpperCase())}</b><div class="muted">${esc(r.created_at?new Date(r.created_at).toLocaleString('es-EC'):'')}</div></div><button class="secondary" id="crCloseDetail" type="button" data-gi=aeccae342e4b>Cerrar</button></div><div class="crNote"><b data-gi=07dc559c3d74>Cliente:</b> ${esc(clientName(r))}${clientEmail(r)?' · '+esc(clientEmail(r)):''}</div>${r.notes?`<div class="crNote"><b data-gi=ba87b30324c7>Comentario:</b> ${esc(r.notes)}</div>`:''}<div class="crLines"><table><thead><tr><th data-gi=77b9238931ed>Producto</th><th data-gi=4805b9ed5d23>Cant.</th><th data-gi=2e4385b6057f>Precio</th><th data-gi=ba6da46c5e1c>IVA</th><th data-gi=4c65f6bb0d0c>Total sin IVA</th></tr></thead><tbody>${lines.map(x=>`<tr><td><b>${esc(product(x.product_id)?.name||'Producto')}</b><small>${esc(product(x.product_id)?.reference||'')}</small></td><td>${esc(x.quantity)}</td><td>${money(x.unit_price)}</td><td>${esc(x.tax_rate)}%</td><td>${money(x.line_total)}</td></tr>`).join('')}</tbody></table><div class="crTotal"><span data-gi=e4a2d805310b>Total solicitado sin IVA</span><span>${money(r.total)}</span></div></div><div class="crActions">${r.invoice_id?'<button class="primary" id="crInvoiceLink" data-gi-live data-gi=66fe2768d8fe>Ver presupuesto</button>':!['cancelled','rejected'].includes(r.status)?'<button class="primary" id="crInvoice" data-gi-live data-gi=b71aae8ecd0b>Crear presupuesto</button><button class="danger" id="crCancel" data-gi=bb9dbb406dcb>Cancelar</button>':''}</div><div id="crDetailMsg" class="crMsg"></div></div>`;document.getElementById('crCloseDetail').onclick=()=>host.innerHTML='';document.getElementById('crCancel')?.addEventListener('click',()=>setStatus(r,'cancelled'));for(const key of ['crInvoice','crInvoiceLink'])document.getElementById(key)?.addEventListener('click',async e=>{const button=e.currentTarget;button.disabled=true;try{await window.GamaQuotes.fromRequest(r.id)}catch(error){const msg=document.getElementById('crDetailMsg');if(msg)msg.textContent=error.message||String(error);else alert(error.message||String(error))}finally{button.disabled=false}})}catch(e){host.innerHTML='<div class="crDetail crErr">Error: '+esc(e.message||e)+'</div>'}}
async function setStatus(r,status){try{const x=await C().update('customer_requests',r.id,{status});if(x.error)throw x.error;r.status=status;render();detail(r.id)}catch(e){const m=document.getElementById('crDetailMsg');if(m){m.className='crMsg crErr';m.textContent='Error: '+(e.message||e)}}}
async function mount(host,id=null){
 if(!staff())return;
 clearTimeout(searchTimer);++generation;host.innerHTML='<div id="gqRequests"></div>';ensure();await load();
 if(!host.isConnected||!document.getElementById('gqRequests')||!staff())return;
 if(id){const result=await C().list('customer_requests',{eq:{id},limit:1});if(result.error)throw result.error;if(!result.data?.[0])throw Error('Solicitud no disponible.');if(!requests.some(x=>x.id===id))requests.push(result.data[0]);await detail(id)}
}
window.GamaCustomerRequests={mount};
window.GamaOpenCustomerRequests=()=>window.GamaQuotes?.openRequests();
window.GamaOpenCustomerRequest=id=>window.GamaQuotes?.openRequests(id);
window.addEventListener('gama:auth-change',()=>{++generation;requests=[];customers=[];products=[];document.getElementById('gqRequests')?.remove()});
})();
