{
  "name": "gama-stock-manager-tests",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests for GAMA Enterprise Resource Planning (static site, no build step)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.3.14",
    "@playwright/test": "^1.47.0"
  }
}
