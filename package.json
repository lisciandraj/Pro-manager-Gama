{
  "name": "gama-stock-manager-tests",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests for GAMA Stock Manager (static site, no build step)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
